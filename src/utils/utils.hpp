#ifndef WS_UTILS_UTILS_HPP
# define WS_UTILS_UTILS_HPP

#include <iostream>
#include <sstream>
#include <vector>
#include <string>

namespace ws
{
	namespace utils
	{

		std::vector<std::string> split(std::string input, char delim);

	}
}

#endif
